import Template from "./_template"
import { Typography, TypographyVariant } from "@dreampipcom/oneiros"
export default async function Page() {
  return <Template title="Ops! Could not tune to this frequency." />
}
