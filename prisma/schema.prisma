// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
  output   = "../generated/prisma"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

model User {
  id            String    @id @default(auto()) @map("_id") @db.ObjectId
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt
  entries       Json?
  settings      Settings?
  analysis      Json?
  availableBalance  String?
  stash         String?   @default("0")
  equity        String?   @default("0")
  totalEarnings String?   @default("0")
  hint String?
  userId        String?   @unique
  lastLogin     DateTime?
  contacts      Contact[]
  things        Thing[]
  lifeEvents    LifeEvent[]
  profile       Profile?
  friends       String[]  @db.ObjectId
  closeFriends  String[]  @db.ObjectId
  friendRequests String[] @db.ObjectId
}

model Contact {
  id            String    @id @default(auto()) @map("_id") @db.ObjectId
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt
  name          String
  email         String?
  phone         String?
  notes         String?
  userId        String    @db.ObjectId
  user          User      @relation(fields: [userId], references: [id], onDelete: Cascade)
  interactionQuality Int? // Rating from 1-5
}

model Thing {
  id            String    @id @default(auto()) @map("_id") @db.ObjectId
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt
  name          String
  notes         String?
  userId        String    @db.ObjectId
  user          User      @relation(fields: [userId], references: [id], onDelete: Cascade)
  interactionQuality Int? // Rating from 1-5
}

model LifeEvent {
  id            String    @id @default(auto()) @map("_id") @db.ObjectId
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt
  name          String
  notes         String?
  userId        String    @db.ObjectId
  user          User      @relation(fields: [userId], references: [id], onDelete: Cascade)
  impact        Int? // Rating from 1-5
}

model Profile {
  id            String    @id @default(auto()) @map("_id") @db.ObjectId
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt
  userId        String    @unique @db.ObjectId
  user          User      @relation(fields: [userId], references: [id], onDelete: Cascade)
  firstName     String?
  lastName      String?
  userName      String?   @unique
  bio           String?
  profilePicture String?
  publicCharts  Json?     // Stores visibility settings for charts
  // Visibility settings for each field
  firstNameVisible     Boolean @default(false)
  lastNameVisible      Boolean @default(false)
  userNameVisible      Boolean @default(false)
  bioVisible           Boolean @default(false)
  profilePictureVisible Boolean @default(false)
  publicChartsVisible  Boolean @default(false)
  // Relations to new collections
  createdTemplates Template[] @relation("TemplateCreator")
  createdTaskLists TaskList[] @relation("TaskListCreator")
  createdNotes     Note[]     @relation("NoteCreator")
}

model Template {
  id            String    @id @default(auto()) @map("_id") @db.ObjectId
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt
  name          String
  description   String?
  tasks         Task[]    // Embedded tasks for the template
  visibility    Visibility
  owners        String[]  @db.ObjectId // References to Profile IDs
  createdBy     String    @db.ObjectId
  creator       Profile   @relation("TemplateCreator", fields: [createdBy], references: [id], onDelete: Cascade)
}

model TaskList {
  id            String    @id @default(auto()) @map("_id") @db.ObjectId
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt
  name          String
  description   String?
  visibility    Visibility
  budget        String?
  dueDate       String?
  owners        String[]  @db.ObjectId // References to Profile IDs
  collaborators String[]  @db.ObjectId // References to Profile IDs
  managers      String[]  @db.ObjectId // References to Profile IDs
  publicUrl     String?
  ticker        String?
  progress      String?
  templateId    String?   @db.ObjectId
  template      Template? @relation(fields: [templateId], references: [id])
  createdBy     String    @db.ObjectId
  creator       Profile   @relation("TaskListCreator", fields: [createdBy], references: [id], onDelete: Cascade)
}

model Note {
  id            String    @id @default(auto()) @map("_id") @db.ObjectId
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt
  title         String
  content       String
  visibility    Visibility
  owners        String[]  @db.ObjectId // References to Profile IDs
  createdBy     String    @db.ObjectId
  creator       Profile   @relation("NoteCreator", fields: [createdBy], references: [id], onDelete: Cascade)
  tags          String[]
  isPinned      Boolean   @default(false)
}

type Settings {
  availableBalance String?
  monthsFixedIncome  String?
  monthsVariableIncome String?
  monthsNeedFixedExpenses String?
  monthsNeedVariableExpenses String?
  dailyTemplateId String?   @db.ObjectId
  weeklyTemplateId String?  @db.ObjectId
}

type Mood {
  gratitude Int
  optimism Int
  restedness Int
  tolerance Int
  selfEsteem Int
  trust Int
  text String
}

type Day {
  tasks Task[]
  ephemeralTasks Task[]
  earnings String
  ticker Float?
  date String
  status String
  mood Mood?
  hint String?
  contacts ContactReference[]
  things ThingReference[]
  lifeEvents LifeEventReference[]
  availableBalance String?
}

type Week {
  week String
  year  Int
  tasks  Task[]
  ephemeralTasks Task[]
  earnings String
  ticker Float?
  date String
  status String
  days Day[]
  hint String?
  availableBalance String?
}

type Task {
  id String?
  name String
  categories Category[]
  area Areas
  status String
  cadence String
  times Int
  count Int?
  localeKey String?
  contacts ContactReference[]
  things ThingReference[]
  favorite Boolean?
  isEphemeral Boolean?
  createdAt String?
}

type ContactReference {
  id String
  name String
  interactionQuality Int?
}

type ThingReference {
  id String
  name String
  interactionQuality Int?
}

type LifeEventReference {
  id String
  name String
  impact Int?
}

enum Areas {
  self
  home
  social
}

enum Category {
  body
  spirituality
  fun
  extra
  clean
  affection
  maintenance
  community
  growth
  work
  mind
  spirit
  social
  home
  custom
  event
}

enum Visibility {
  public
  private
  friends
  closeFriends
}